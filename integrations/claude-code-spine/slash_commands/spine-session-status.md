---
description: Show the status, drift score, and recent evaluations for the active Spine session.
allowed-tools: Bash(python3:*)
---

Show the active Spine plan-bound session's drift score, goal, evaluation count,
and the last few alignment verdicts.

```bash
python3 ~/.spine/spine-session-cli.py status
```
