---
description: End the active Spine plan-bound session.
allowed-tools: Bash(python3:*)
---

End the current Spine plan-bound session and print the final drift score.

```bash
python3 ~/.spine/spine-session-cli.py end
```
